language: node_js
os:
  - unix
node_js:
  - "6"
  - "6.1"
after_success: 'npm run coveralls'

# whitelist
branches:
  only:
    - master

# services: # enable various services
#   - mongodb     # will start mongodb

# install:
#   - npm install -g bower
#   - npm install
#   - bower install

# script: npm start
